= flash[:error] 
#content
  %h2 Account
  = render :partial=>"navi"
  .account_form
    %h4 Set API Auth Key
    -if @member.auth_key
      %p 
        Your API Auth Key is:
        =@member.auth_key
    %p
      =form_tag do
        =submit_tag 'Set or Change Auth Key'
