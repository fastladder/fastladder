rvm:
- 1.9.3
- 2.0.0
branches:
  only:
    - master
before_script:
  - 'cp config/database.yml.sqlite3 config/database.yml'
  - 'RAILS_ENV=test bundle exec rake setup'
script: 'RAILS_ENV=test bundle exec rake db:drop db:create db:migrate spec && RAILS_ENV=production bundle exec rake assets:precompile'
